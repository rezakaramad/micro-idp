{{- if .Values.githubProvider.enabled }}
# This will allow Crossplane to create deployment repositories in 'fluxdojo' GitHub organization.

apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: github-app-fluxdojo
  namespace: crossplane
spec:
  refreshInterval: 20m
  secretStoreRef:
    name: vault-local
    kind: ClusterSecretStore
  target:
    name: github-app-fluxdojo
    creationPolicy: Owner
    template:
      engineVersion: v2
      type: Opaque
      mergePolicy: Replace
      data:
        credentials: |
          {
            "app_auth": [{
              "id": "{{`{{ .app_id }}`}}",
              "installation_id": "{{`{{ .installation_id }}`}}",
              "pem_file": {{`{{ .pem | toJson }}`}}
            }],
            "owner": "fluxdojo"
          }
  data:
    - secretKey: app_id
      remoteRef:
        key: management/github/apps/crossplane-managed-github-repos
        property: app-id
    - secretKey: installation_id
      remoteRef:
        key: management/github/apps/crossplane-managed-github-repos
        property: installation-id
    - secretKey: pem
      remoteRef:
        key: management/github/apps/crossplane-managed-github-repos
        property: private-key

---
{{- end }}
